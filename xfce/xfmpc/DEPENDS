depends glib2 &&
depends gtk+3 &&
depends libxfce4util &&
depends libxfce4ui &&
depends libmpd &&

optional_depends GETTEXT \
                 "--enable-nls" \
                 "--disable-nls" \
                 "for Native Language Support"
